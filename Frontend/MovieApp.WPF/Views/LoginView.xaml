<UserControl x:Class="MovieApp.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300" Margin="40">
            <TextBlock Text="Iniciar Sesion" FontSize="24" Margin="0 0 0 20" TextAlignment="Center"/>

            <TextBlock Text="Correo electronico" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 10"/>

            <TextBlock Text="Contrasena" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0 10 0 0"/>

            <Button Content="Login" Command="{Binding LoginCommand}" />
            <Button Content="No tienes cuenta, Registrate" Command="{Binding GoToRegisterCommand}" Margin="0 10 0 0"/>

        </StackPanel>
    </Grid>
</UserControl>
