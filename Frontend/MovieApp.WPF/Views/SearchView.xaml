<UserControl x:Class="MovieApp.WPF.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 0 0 20">
                <TextBox Width="300" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 10 0" />
                <Button Content="Buscar" Command="{Binding SearchCommand}" Background="#FF626FD6" Foreground="#FFFDF6F6" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </StackPanel>

            <ListBox ItemsSource="{Binding SearchResults}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0 0 0 10">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="{Binding Poster}" Width="50" Height="75" Margin="0 0 10 0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Year}" />
                                </StackPanel>
                                <Button Content="❤️"
                                        Command="{Binding DataContext.AddFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" 
                                        Margin="20 0 0 0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
